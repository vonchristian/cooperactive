<h2>New entry</h2>

<%= render "form.html", changeset: @changeset, accounts: @accounts,
                        action: entry_path(@conn, :create) %>

<%= link "Back", to: entry_path(@conn, :index) %>
